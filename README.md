# OSINT Reconnaissance Framework

A simple, minimal Node.js framework for aggregating Open Source Intelligence (OSINT) data for a given target (Domain, Email, IP, or Username
Im vansh editing this
UI looks cleaner with the spacing tweaks 👍

One suggestion: the sidebar navigation could benefit from more visual hierarchy. Right now, items feel a bit flat. You could improve this by:

* Adding a subtle hover/active background for `.nav-btn`
* Increasing contrast between active and inactive items
* Slightly adjusting spacing consistency (gap + padding) for better alignment
* Maybe adding icons or a divider (like `.nav-divider`) to group sections more clearly

This would make navigation more intuitive and visually structured.

